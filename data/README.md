# Data

Only sanitized, reproducible development fixtures belong here. The authoritative
source JSON remains read-only in the sibling data repository.

`fixtures/` contains small, synthetic datasets that preserve the source JSON
shapes without copying personal records. Use these fixtures for local tests and
examples; ingestion against the authoritative data is a separate workflow.
